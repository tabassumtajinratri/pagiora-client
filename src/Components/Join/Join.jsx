import join from '../../assets/join.png';

const Join = () => {
  return (
    <div
      className="text-5xl p-32 text-white bg-cover bg-center h-screen"
      style={{
        backgroundImage: `url(${join})`,
      }}
    >
    </div>
  );
};

export default Join;
